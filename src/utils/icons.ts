export function getIconComponent(iconName: string) {
  // This is a helper for mapping icon names to react-icons components
  // Since we're using Astro, we'll just return the icon name and handle it in the components
  return iconName;
}
